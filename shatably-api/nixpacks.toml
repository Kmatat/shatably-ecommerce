# Nixpacks configuration for Railway
# Use npm install instead of npm ci to handle package.json changes

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
