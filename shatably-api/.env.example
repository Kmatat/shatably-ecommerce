# Database (Use Supabase free tier or Railway)
# IMPORTANT: ?sslmode=require is mandatory for Supabase connections
# For Supabase, use the connection pooler URL with ?pgbouncer=true&sslmode=require
# Format: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true&sslmode=require
DATABASE_URL="postgresql://postgres:password@db.xxxxx.supabase.co:5432/postgres?sslmode=require"

# Direct URL for Prisma migrations (use direct connection, not pooler)
DIRECT_URL="postgresql://postgres:password@db.xxxxx.supabase.co:5432/postgres?sslmode=require"

# JWT Secret (generate a strong random string)
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_EXPIRES_IN="7d"

# Server
PORT=3001
NODE_ENV="development"
FRONTEND_URL="http://localhost:3000"

# =============================================================================
# OTP CONFIGURATION
# =============================================================================
# OTP Settings
OTP_EXPIRY_MINUTES=5
OTP_LENGTH=6

# OTP Provider Options: console, email, smsmisr, unifonic
# - console: Logs OTP to terminal (development only)
# - email: FREE - sends OTP via email (recommended for production)
# - smsmisr: Paid SMS service for Egypt
# - unifonic: Paid SMS service for Egypt
OTP_PROVIDER="console"

# -----------------------------------------------------------------------------
# EMAIL OTP (FREE - Recommended for Production)
# -----------------------------------------------------------------------------
# Option 1: Gmail SMTP (FREE)
# 1. Go to Google Account > Security > 2-Step Verification
# 2. Generate an "App Password" for Mail
# 3. Use your Gmail address and the app password below
#
# Option 2: Brevo/Sendinblue (FREE - 300 emails/day)
# 1. Sign up at https://www.brevo.com
# 2. Go to SMTP & API > SMTP settings
# 3. Use smtp-relay.brevo.com as host
#
# Option 3: Mailgun (FREE - 5,000 emails/month for 3 months)
# 1. Sign up at https://www.mailgun.com
# 2. Verify your domain
# 3. Use smtp.mailgun.org as host

SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="noreply@shatably.com"

# -----------------------------------------------------------------------------
# SMS PROVIDERS (PAID)
# -----------------------------------------------------------------------------
# SMS Misr (Egypt) - https://smsmisr.com
# Cost: ~0.08 EGP per SMS
SMSMISR_USERNAME=""
SMSMISR_PASSWORD=""
SMSMISR_SENDER_TOKEN=""
SMSMISR_TEMPLATE_TOKEN=""
SMSMISR_ENVIRONMENT="2"  # 1=Live, 2=Test

# Unifonic (alternative) - https://www.unifonic.com
# Cost: ~0.02-0.05 EGP per SMS
UNIFONIC_APP_SID=""
UNIFONIC_SENDER_ID="Shatably"

# =============================================================================
# FILE UPLOAD
# =============================================================================
# Cloudinary (FREE tier: 25GB storage, 25GB bandwidth/month)
# Get free account at: https://cloudinary.com
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""

# =============================================================================
# PAYMENT GATEWAYS
# =============================================================================
# Paymob (Egypt's leading gateway)
# Register at: https://paymob.com
PAYMOB_API_KEY=""
PAYMOB_INTEGRATION_ID=""
PAYMOB_IFRAME_ID=""
PAYMOB_HMAC_SECRET=""

# Fawry Payment (optional)
FAWRY_MERCHANT_CODE=""
FAWRY_SECURITY_KEY=""

# =============================================================================
# OTHER SERVICES
# =============================================================================
# Google Maps (for delivery tracking - optional)
GOOGLE_MAPS_API_KEY=""

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
